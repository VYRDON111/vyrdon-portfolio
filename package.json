{
  "name": "vyrdon-portfolio",
  "version": "0.2.0",
  "private": true,
  "description": "VYRDON organizational portfolio — system overview, console, review forum. Static HTML/CSS, no build step.",
  "scripts": {
    "serve": "npx serve@latest -l 5173 .",
    "serve:console": "npx serve@latest -l 5174 console/",
    "serve:forum": "npx serve@latest -l 5175 vyrdon-forum/",
    "check:links": "command -v linkchecker >/dev/null && linkchecker http://localhost:5173 || echo 'linkchecker not installed; skipping'"
  },
  "engines": {
    "node": ">=18"
  }
}